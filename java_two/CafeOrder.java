/**
 * An order is placed by a customer, created by a cashier, and served by a barista
 * It's added to a queue
 */

public class CafeOrder {
    // String itemName;
    // double itemPrice;
    
    CafeOrder(int orderID) {
        // Creates a new order
        addOrderToList(orderID++);
    }
    
    void addOrderToList(int id) {
        // Adds new order to queue of orders
    }
}
